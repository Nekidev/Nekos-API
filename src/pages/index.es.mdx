import React from 'react';

import Link from 'next/link';
import styles from '../styles/Home.module.css';

import { Tab, Tabs } from 'nextra-theme-docs';

import { FaceSmileIcon, CurrencyDollarIcon, PhotoIcon, BoltIcon } from '@heroicons/react/24/outline'
import CodePre from '../components/codepre/CodePre';

export const features = [
    {
        title: "Facil de usar",
        description: "La API es muy facil de usar y de implementar en proyectos existentes.",
        icon: <FaceSmileIcon />
    },
    {
        title: "Gratis para siempre",
        description: "No es necesario pagar nada, aqui es todo gratis. (aunque felizmente aceptaremos donaciones)",
        icon: <CurrencyDollarIcon />
    },
    {
        title: "Miles de imagenes",
        description: "Mas de 2000 imagenes y mas agregadas todos los dias.",
        icon: <PhotoIcon />
    },
    {
        title: "Veloz",
        description: "La API es muy rapida. Le preguntamos al creador de la luz y dijo que es la cosa mas rapida quee ha visto.",
        icon: <BoltIcon />
    }
];

export default function Home() {
    return (
        <div className={styles.main}>
            <div className={styles.content}>
                <div className={styles.heroContainer}>
                    <h1 className={styles.mainTitle}>Nekos API</h1>
                    <p className={styles.mainDescription}>Una API de imagenes de anime gratis, de codigo abierto, y publica.</p>
                    <Link href="/docs" className={styles.mainLink}>
                        Documentación
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" strokeWidth={1.5} stroke="currentColor">
                            <path strokeLinecap="round" strokeLinejoin="round" d="M13.5 4.5L21 12m0 0l-7.5 7.5M21 12H3" />
                        </svg>
                    </Link>
                </div>
            </div>
            <div className={`${styles.content} ${styles.infoContainer}`}>
                <h2 className={styles.infoTitle}>Características</h2>
                <div className={styles.infoCards}>
                    {features.map((feature, i) => (
                        <Card 
                            key={i}
                            title={feature.title}
                            description={feature.description}
                            icon={feature.icon} />
                    ))}
                </div>
            </div>
            <div className={`${styles.content} ${styles.infoContainer} ${styles.wrappersContainer}`}>
                <div>
                    <h2 className={styles.infoTitle}>Librerias oficiales</h2>
                    <p className={styles.infoSubtitle}>
                        Integra facilmente nuestra API a tus proyectos con una de nuestras librerias oficiales.
                    </p>
                </div>
                <div style={{ marginTop: "-1rem" }}>
                    <Tabs items={['Python', 'JavaScript']}>
                        <Tab>
                            <CodePre language="python">
                                {`from anime_api.apis import NekosAPI\n\nnekos = NekosAPI()\n\nnekos.get_random_image()`}
                            </CodePre>
                        </Tab>
                        <Tab>
                            <CodePre language="javascript">
                                {`const { NekosAPI } = require('nekosapi');\n\nconst nekos = new NekosAPI();\n\nnekos.getRandomImage().then(image => console.log(image));`}
                            </CodePre>
                        </Tab>
                    </Tabs>
                </div>
            </div>
            <div className={`${styles.content} ${styles.infoContainer} grid grid-cols-1 md:grid-cols-2 gap-16 relative`}>
                <div>
                    <h2 className={styles.infoTitle}>Unete a nuestra comunidad</h2>
                    <p className={styles.infoSubtitle}>
                        Unete a nuestra comunidad en Discord para obtener soporte, hablar de la API, y mas.
                    </p>
                    <Link className={styles.discordBanner} href="https://discord.gg/b9Fv3kEfXc">
                        <img src="https://discordapp.com/api/guilds/1023471762723328010/widget.png?style=banner2" />
                    </Link>
                </div>
                <div>
                    <h2 className={styles.infoTitle}>Haz una donación</h2>
                    <p className={styles.infoSubtitle}>
                        Si te gusta nuestra API, considera hacer una donacion para ayudarnos a mantenerla.
                    </p>
                    <Link className={styles.patreonLink} href="https://patreon.com/nekidev">
                        <img src="https://c5.patreon.com/external/logo/become_a_patron_button.png" />
                    </Link>
                </div>
            </div>
        </div>
    )
}

export function Card({ title, description, icon }) {
    return (
        <div className={styles.cardContainer}>
            <div className={styles.iconContainer}>
                {icon}
            </div>
            <div>
                <div className={styles.title}>{title}</div>
                <div className={styles.description}>{description}</div>
            </div>
        </div>
    )
}

<Home />