import { Callout, Tab, Tabs } from 'nextra-theme-docs';

# JavaScript

<Callout type="info">
    This page is a work in progress.
</Callout>

The [`nekosapi`](https://www.npmjs.com/package/nekosapi) Typescript library is a officially supported API warapper for Nekos API, kindly made by [Cataclym](https://gitlab.com/cataclym).

## Installation

The library can be installed using Yarn, PNPM or NPM:


<Tabs items={['Yarn', 'PNPM', 'NPM']}>
    <Tab>
        ```bash
        yarn add nekosapi
        ```
    </Tab>
    <Tab>
        ```bash
        pnpm add nekosapi
        ```
    </Tab>
    <Tab>
        ```bash
        npm install nekosapi
        ```
    </Tab>
</Tabs>

## Usage

The `NekosAPI(){:javascript}` class can be imported from the `nekosapi` package.

```javascript showLineNumbers
const { NekosAPI } = require('nekosapi');

const nekos = new NekosAPI();
```
